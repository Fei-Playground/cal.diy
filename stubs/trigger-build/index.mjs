// ESM stub for @trigger.dev/build — build extensions are no-ops.
const ext = () => ({});
export const syncVercelEnvVars = ext;
export const additionalPackages = ext;
export const additionalFiles = ext;
export const esbuildPlugin = ext;
export const emitDecoratorMetadata = ext;
export const prismaExtension = ext;
export default { syncVercelEnvVars, additionalPackages, additionalFiles, esbuildPlugin, emitDecoratorMetadata, prismaExtension };
