{
  "name": "@calcom/emails",
  "sideEffects": false,
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dx": "docker compose up -d || docker-compose up -d"
  },
  "dependencies": {
    "@calcom/dayjs": "portal:../dayjs",
    "@calcom/lib": "portal:../lib",
    "rrule": "2.7.1"
  },
  "peerDependencies": {
    "react": "^18.0.0 || ^19.0.0",
    "react-dom": "^18.0.0 || ^19.0.0"
  },
  "devDependencies": {
    "@calcom/testing": "portal:../testing",
    "@calcom/tsconfig": "portal:../tsconfig",
    "@calcom/types": "portal:../types"
  }
}
